import{randomUUID}from'node:crypto';
import{seal,setCookie,callbackUrl,home}from'../_instagram.js';
export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});if(!process.env.META_APP_ID||!process.env.META_APP_SECRET||!process.env.INSTAGRAM_SESSION_SECRET)return res.redirect(302,home(req,'setup-required'));let state=randomUUID(),url=new URL('https://www.instagram.com/oauth/authorize');url.searchParams.set('client_id',process.env.META_APP_ID);url.searchParams.set('redirect_uri',callbackUrl(req));url.searchParams.set('response_type','code');url.searchParams.set('scope','instagram_business_basic,instagram_business_content_publish');res.setHeader('Set-Cookie',setCookie('scs_instagram_state',seal({state,createdAt:Date.now()}),600));return res.redirect(302,url.toString())}
